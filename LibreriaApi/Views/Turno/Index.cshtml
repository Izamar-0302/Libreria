@model IEnumerable<LibreriaApi.Models.Turno>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Horainicio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Horafinal)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Horainicio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Horafinal)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TurnoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TurnoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TurnoId })
        </td>
    </tr>
}

</table>
